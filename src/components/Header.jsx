import TopHeader from './TopHeader';
import MainNavbar from './MainNavbar';

const Header = () => {
  return (
    <header>
      <TopHeader />
      <MainNavbar />
    </header>
  );
};

export default Header;